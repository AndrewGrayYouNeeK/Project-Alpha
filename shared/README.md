Shared types and runtime schemas consumed by client and server.

Includes `RealtimeEvent` validation and Redis-backed `EntityRecord` types used by the self-hosted entity API (Base44-free replacement).
